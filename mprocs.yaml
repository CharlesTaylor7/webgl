procs:
  Wasm Pack:
    shell: watchexec -e rs -- wasm-pack build --debug --target web

  Dev Server:
    shell: npm run dev

  Docs:
    shell: cargo doc --open
    autostart: false

  Unit Tests:
    shell: cargo watch -x test
    autostart: false
